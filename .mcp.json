{
  "mcpServers": {
    "marconi": {
      "command": "uv",
      "args": [
        "--directory",
        "${CLAUDE_PLUGIN_ROOT}",
        "run",
        "--extra",
        "mcp",
        "marconi-mcp"
      ]
    }
  }
}
